using System.Runtime.CompilerServices;

namespace PropertyGridHelpers.ServiceProviders
{
    /// <summary>
    /// The <c>PropertyGridHelpers.ServiceProviders</c> namespace provides infrastructure for 
    /// working with <see cref="System.IServiceProvider"/> in scenarios where lightweight or 
    /// ad-hoc service resolution is needed. It includes the <c>CustomServiceProvider</c>, a 
    /// simple implementation of <see cref="System.IServiceProvider"/> that can be used to manually 
    /// register and resolve services without relying on a full dependency injection framework.
    ///
    /// This is useful for unit testing or for using core functionality from the 
    /// <c>PropertyGridHelpers</c> library without relying on attributes or requiring 
    /// integration with a visual property grid control.
    /// </summary>
    [CompilerGenerated]
    public class NamespaceDoc
    {
    }
}
